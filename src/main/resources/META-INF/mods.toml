modLoader="javafml"
loaderVersion="[31,)"

issueTrackerURL="https://github.com/Wimpingego/nnow/issues"

[[mods]]

modId="nnow"
version="0.1.5"
displayName="Never Needed Or Wanted"
#updateJSONURL="http://myurl.me/"
displayURL="https://github.com/Wimpingego"
logoFile="modimage.png"
credits="WimpingEgo"
authors="WimpingEgo"
description='Adds well something to the game, Well I hope it does.'

[[dependencies.nnow]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[31.1.78,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"

[[dependencies.nnow]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.15.2]"
    ordering="NONE"
    side="BOTH"
